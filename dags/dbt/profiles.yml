stock_screening_portfolio:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: clean_stock_data
      threads: 4 
      location: US